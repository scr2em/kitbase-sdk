---
"@kitbase/flags": minor
---

Add IIFE bundle for browser/CDN usage with bundled OpenFeature web SDK. The bundle is exposed via `https://unpkg.com/@kitbase/flags` (also available on jsDelivr) and registers the `KitbaseFlags` global containing `FlagsClient`, `KitbaseProvider`, and `OpenFeature`.
